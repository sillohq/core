"""The application under test, one module per framework.

Each is written the way that framework's own documentation would write it, and
all three import their payloads from :mod:`sillo_bench.payloads` so no
framework is quietly serving a different object than another.
"""
