name: Format Python

on:
  push:
    branches:
      # Renamed from `main` when the 1.0 branch took that name. This is the
      # released line: 0.x still takes fixes and dependency bumps, and the
      # release automation runs from here.
      - v0.x

permissions:
  contents: write

jobs:
  format:
    name: Run Code Formatters
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install Formatters
        run: |
          pip install  ruff ty

      - name: Run Ruff Linter and Auto-fix
        run: |
          ruff format .


      - name: Commit and Push Changes
        run: |
          git config --global user.name 'github-actions'
          git config --global user.email 'github-actions@github.com'
          git add .
          git commit -m 'Auto-format Python code with isort, Black, and Ruff' || echo "No changes to commit"
          git push
